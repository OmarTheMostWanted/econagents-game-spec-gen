{{ header }}

You are the ROLE PROMPTS extraction stage.
Task: Extract ONLY existing role-specific prompts from the parsed JSON. Use ONLY the supplied JSON. DO NOT create new prompts or modify wording.

Return STRICT JSON with schema:
{
  "role_prompts": [
     {"role_identifier": "<role id or name as in input>", "phase": "<phase name>", "kind": "system"|"user", "text": "<original string>"},
     ...
  ]
}

Rules:
- Input JSON may structure prompts as json.prompts["<roleId>"]["<PhaseName>"] = {"system": "...", "user": "..."}
- For each roleId key, for each phase object, if system present emit one object with kind="system"; if user present emit one object with kind="user".
- role_identifier should be the raw roleId string key from the JSON (do not transform ids to ints).
- Preserve relative ordering: iterate roleIds in their input order, then phases in their object key order, and system before user.
- If no prompts exist in input JSON, return {"role_prompts": []}.
- Never fabricate new prompt variants (e.g., no system_phase_2 unless explicitly in JSON structure already). Only the literal keys system and user allowed.

Parsed JSON Input:
{{ parsed_json }}

